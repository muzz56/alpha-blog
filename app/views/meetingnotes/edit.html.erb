<h1>Edit Meeting Notes </h1>

<%= form_with(model: @meetingnote, local: true) do |f| %>
    <p>
        <%= f.label :notes %> <br>
        <%= f.text_area :description %>
    </p>
    <p>
        <%= f.submit %>
    </p>
<% end %>